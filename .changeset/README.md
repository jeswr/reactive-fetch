# Changesets

This directory is managed by [changesets](https://github.com/changesets/changesets).

- Run `pnpm changeset` to record a change.
- Run `pnpm version-packages` to bump versions and update changelogs from the recorded changes.
- Run `pnpm release` to build and publish.
